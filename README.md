# Botpress Webhook Relay

This is a lightweight FastAPI server that receives incoming webhooks (e.g. from Meta/Facebook) and relays them to your Botpress webhook.

## Environment Variables

Create a `.env` file:

```bash
cp .env.example .env
